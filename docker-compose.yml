version: "3.9"

services:
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: web       # Accessible inside network as http://dcs:80
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true
